# =============================================================================
# EduPhysics Academy - Docker Environment Variables
# =============================================================================
# HOW TO USE:
#   1. Copy this file:  copy .env.example .env   (Windows)
#                  or:  cp .env.example .env      (Mac/Linux)
#   2. Fill in your real values below
#   3. Run:             docker compose up -d
#
# The .env file is loaded automatically by Docker Compose.
# NEVER commit the .env file — it is listed in .gitignore and .dockerignore.
# =============================================================================

# ---- MongoDB Configuration ----
# Option A: Use the local MongoDB container (default, no changes needed)
MONGODB_URI=mongodb://eduphysics_user:eduphysics_password@mongo:27017/eduphysics

# Option B: Use MongoDB Atlas (cloud) — uncomment and fill in your Atlas URI
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/eduphysics?retryWrites=true&w=majority

# Local MongoDB root credentials (used to initialise the mongo container)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=adminpassword

# ---- Flask Configuration ----
FLASK_ENV=production
PORT=5000

# ---- CORS ----
# The URL where the frontend is accessible (used for CORS allow-list)
FRONTEND_URL=http://localhost

# ---- Cloudinary (Optional) ----
# Only required if your backend handles image uploads to Cloudinary
# Leave blank to use direct frontend-to-Cloudinary uploads
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
